---
- hosts: Router
  gather_facts: no
  connection: network_cli

  tasks:
  - name: Configure a interface on Cisco router
    cisco.ios.ios_config:
     parents: interface fa0/1
     lines: 
       - ip add 10.10.10.10 255.255.255.0
       - no sh

